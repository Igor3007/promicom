mixin callbackPopup
    .popup#callback
        .popup__wrapper
            .popup__close
                a(href="#close"): +icons('#ic_close', 24)
            .popup__content
                .popup__title Оставить заявку
                form(action="").form
                    .form__field
                        +input({
                            type: 'text',
                            placeholder: "Ваше имя",
                            required: true,
                        })
                    .form__field
                        +input({
                            type: 'tel',
                            placeholder: "Ваш телефон",
                            required: true,
                        })
                    .form__submit
                        button.btn.btn-primary(type="submit") Отправить
                    .form__agreement Нажимая на кнопку, вы соглашаетесь на <a href="#">обработку персональных данных</a>
